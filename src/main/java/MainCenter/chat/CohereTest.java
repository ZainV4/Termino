package MainCenter.chat;

import com.cohere.api.Cohere;
import com.cohere.api.requests.ChatRequest;
import com.cohere.api.types.NonStreamedChatResponse;

public class CohereTest {
    public static void main(String[] args) {
        // Create a Cohere client (replace with your actual API key)
        // String apiKey = "your_api_key_here";
        // Cohere cohere = Cohere.builder().token(apiKey).clientName("TestClient").build();
        
        // try {
        //     // Example direct usage of ChatRequest builder
        //     ChatRequest request = ChatRequest.builder()
        //         .token(apiKey)
        //         .message("Hello, Cohere!")
        //         .build();
            
        //     NonStreamedChatResponse response = cohere.chat(request);
        //     System.out.println("Response: " + response.getText());
            
        // } catch (Exception e) {
        //     e.printStackTrace();
        //     System.err.println("Error: " + e.getMessage());
        // }
    }
}
